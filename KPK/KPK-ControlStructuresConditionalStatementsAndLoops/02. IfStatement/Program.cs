namespace _02.IfStatement
{
using System;

    class Program
    {
        static void Main(string[] args)
        {
            public Potato potato;

            if (potato != null && !potato.HasNotBeenPeeled && !potato.IsRotten)
            {
	            Cook(potato);
            }

            if (x >= MIN_X && (x <= MAX_X && ((MAX_Y >= y && MIN_Y <= y) && !shouldNotVisitCell)))
            {
               VisitCell();
            }
        }
    }
}
